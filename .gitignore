# ---- macOS junk ----
.DS_Store

# ---- Build artifacts & distributables ----
# Compiled apps and installers are large and regenerable — ship them via
# GitHub Releases, not in the repo.
CADENCE_Desktop_App/
distribution/
release/
*.app/
*.dmg
*.exe
*.pkg
*.msi

# MATLAB Compiler / deployment scratch
codegen/
slprj/
*.log

# ---- MkDocs build output (published to the gh-pages branch) ----
site/

# ---- Local tooling / secrets ----
.claude/
github-recovery-codes.txt
*recovery-codes*
*.pem
*.p12
*.key

# ---- Research data: never commit recordings ----
*.mat
*.gsd
*.gsh

# ---- Large design sources (the app uses the PNGs; keep .ai out of git) ----
*.ai

# ---- App Designer / MATLAB backups ----
*.mlapp.bak*
*.asv
*.autosave

# MATLAB Project metadata (not used)
Cadence2026.prj
resources/

# ---- R ----
.Rhistory

# ---- temporary figure-panel output ----
feature_extraction_helper/Ui/

# Temporary manuscript figure-panel scripts (to be removed later)
feature_extraction_helper/export_alternans_panels.m
feature_extraction_helper/export_scaled_panel_group.m
feature_extraction_helper/make_manuscript_alternans_panels.m
signal_conditioning_helper/figure2.m

# ---- reference PDFs (copyrighted; not for redistribution) ----
docs/literature/
